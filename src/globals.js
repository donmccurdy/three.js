/* eslint-disable no-undef */

export const self = typeof self === 'undefined' ? null : self;
export const window = typeof window === 'undefined' ? null : window;
export const document = typeof document === 'undefined' ? null : document;
export const navigator = typeof navigator === 'undefined' ? null : navigator;
